
<project>
    <import file="${common.dir}/taskExt.xml" />
    <import file="${common.dir}/xipVersion.xml" />
    <import file="${common.dir}/version.xml" />
    <import file="${common.dir}/properties.xml" />
    <import file="${common.dir}/debug.xml" />
    <import file="${common.dir}/ftp.xml" />
    <import file="${common.dir}/cvs.xml" />
    <import file="${common.dir}/archive.xml" />
    <import file="${common.dir}/test.xml" />
    <import file="${common.dir}/platformProperties.xml" />
    <import file="${common.dir}/clean.xml" />
    <import file="${common.dir}/visualStudio2005Manifest.xml" />
    <import file="${common.dir}/taskSync.xml" />
        <!-- <import file="${common.dir}/fstOrTrans.xml" /> -->
</project>
