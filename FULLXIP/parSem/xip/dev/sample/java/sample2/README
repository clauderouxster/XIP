This example shows how a java application can load the english grammar using a grm file and parse a string.
It provides also how to load some extension grammar files (to the english grammmar).
For more information about the java Api:  .../xip/java/doc/index.html.
See the classes: JXip, Const 
