
<project name="xipWS" default="default" basedir=".">
    <property name="env" environment="env" value="env"/>

    <target name="client" depends="client.compile" description="" />

    <target name="client.compile" depends="" description="">
    </target>

    <target name="server" depends="server.compile" description="" />

    <target name="server.compile" depends="" description="">
    </target>
    
</project>