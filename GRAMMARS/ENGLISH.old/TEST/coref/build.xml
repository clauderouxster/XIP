<project name="" default="test" basedir="." >

<property name="env" environment="env"  value="env"/>
<property name="fs" value="${file.separator}" />

<import file="${basedir}${fs}..${fs}..${fs}..${fs}TESTCOMMON${fs}testDir.xml" />

</project>

