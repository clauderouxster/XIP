//!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
//! Xerox Research Centre Europe - Grenoble Laboratory
//!
//! Copyright (c) 2009 Xerox Corporation.  All Rights Reserved.
//!
//! --- CONTENTS ---
//!   Project    : English XIP grammar for EERQI
//!   filename   : dependency-irms_eerqi.xip
//!   Date       : 2009
//!   Author     : Agnes Sandor
//!  
//!
//!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!

DependencyRules:


/////////////////////
// KEYWORDS
////////////////////

/

//begin article 1. : BJES_Barrow

| #1[lemme:body] |
//if ( ~KEYWORD(#1) & ( $1[contribution](#2) | $2[contribution](#3,#4) ) )
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:character] |
//if ( ~KEYWORD(#1) & ( $1[contribution](#2) | $2[contribution](#3,#4) ))
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:education] |
//if ( ~KEYWORD(#1) & ( $1[contribution](#2) | $2[contribution](#3,#4) ))
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:health] |
//if ( ~KEYWORD(#1) &  ( $1[contribution](#2) | $2[contribution](#3,#4) ))
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:mind] |
//if ( ~KEYWORD(#1) & ( $1[contribution](#2) | $2[contribution](#3,#4) ))
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:movement] |
//if ( ~KEYWORD(#1) & ( $1[contribution](#2) | $2[contribution](#3,#4) ))
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[surface:schooling] |
//if ( ~KEYWORD(#1) &  ( $1[contribution](#2) | $2[contribution](#3,#4) ))
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:sport] |
//if ( ~KEYWORD(#1) & ( $1[contribution](#2) | $2[contribution](#3,#4) ))
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[surface:understanding] |
//if ( ~KEYWORD(#1) & ( $1[contribution](#2) | $2[contribution](#3,#4) ) )
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

//end article 1.



// begin article 2 : BJES_Croll

//attitudes to school, participation rates, gender, socio-economic status, factor analysis

| #1[lemme:attitude] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:school] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:participation] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:rate] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:gender] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:socio] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:economic] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:status] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:factor] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:analysis] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


// end article 2




// begin article 3 : BJES_Kennedy
//professional development, professional learning, CPD policy, key informants, Scotland

| #1[lemme:professional] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:development] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[surface:learning] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:CPD] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:policy] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:key] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:informant] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:Scotland] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


// end article 3



// begin article 4 : BJES_Kupfer

//education policy, governmentality, Bologna Process

| #1[lemme:education] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


| #1[lemme:policy] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


| #1[lemme:governmentality] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


| #1[lemme:bologna] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:process] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)



// end article 4



// begin article 5: BJES_Simmons

//Keywords: school leaving age, NEET, social inclusion, compulsory education.

| #1[lemme:school] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:leave] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:leaving] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:age] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:NEET] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:social] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:inclusion] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:compulsory] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:education] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


// end article 5



// begin article 6: EPR_Dinsmore

//Keywords: Metacognition, Self-regulation,  Self-regulated learning.

| #1[lemme:metacognition] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:Metacognition] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:self] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:regulation] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:regulate] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:regulated] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:learning] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:learn] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


// end article 6: 





// begin article 7: EPR_Fox

//Keywords: Metacognition, Self-regulation.

| #1[lemme:metacognition] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:Metacognition] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:self] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:regulation] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)



// end article 7

if ( ^KEYWORD(#1) & KEYWORD(#2) & #1::#2 )
  ~.





// begin article 8: EPR_Lajoie

//Keywords:  Metacogniton, Self-regulation, Self-regulated learning, Computer based learning environments

| #1[lemme:metacognition] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:Metacognition] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:self] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:regulation] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:regulate] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:regulated] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:learning] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:learn] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:computer] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:environment] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[surface:CBLEs] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[surface:CBLE] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)



// end article 8



// begin article 9: EPR_Maggioni

//Keywords:  Epistemic cognition, Epistemic beliefs, Calibration, Instruction, Teachers.

| #1[lemme:epistemic] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:Epistemic] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:cognition] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:belief] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:calibration] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:instruction] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:teacher] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


// end article 9



// begin article 10: EPR_Winters

//Keywords Self-regulated learning, Computer learning environments.


| #1[lemme:self] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:regulate] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:regulated] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:learning] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:learn] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:computer] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:environment] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[surface:CBLEs] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[surface:CBLE] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)



// end article 10




// begin article 13: IJER_Molander

//Keywords: Science, Deaf, Mediation, Sign language, Communication.

| #1[lemme:science] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:deaf] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:mediation] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:sign] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:language] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:communication] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


// end article 13]


// begin our article: evaluation, peer-review, research, natural language processing, NLP, structure

| #1[lemme:evaluation] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:peer] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:review] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:research] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:natural] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:language] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[surface:processing] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:NLP] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:structure] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

// end our article



// begin Bridges:Impossible science

//RESEARCH QUALITY ASSESSMENT IN EDUCATION: IMPOSSSIBLE SCIENCE, POSSIBLE ART?


| #1[lemme:research] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:quality] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


| #1[lemme:assessment] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:education] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:science] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:art] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

// end  Bridges:Impossible science



// begin On the Necessity of Radical State Education: Democracy and the Common School.




| #1[lemme:necessity] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:radical] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:state] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:education] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:democracy] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:common] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

| #1[lemme:school] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)

// end On the Necessity of Radical State Education: Democracy and the Common School.

\

/
/////////////////////////////////////////////
// not necessary


if ( KEYWORD(#1) & ^$1[contribution](#2) )
   $1[topic=+](#2).

if ( KEYWORD(#1) & ^$1[contribution](#2,#3) )
   $1[topic=+](#2,#3).

| #1[topicword=+] |
if ( KEYWORD(#1) )
   ~.

if ( ^KEYWORD(#1) & KEYWORD(#1) )
   ~.





if ( KEYWORD(#1) & ^SUMMARY[topic:~](#2) )
   SUMMARY[topic=+](#2).
  
if ( KEYWORD(#1) & ^IMPSUMMARY[topic:~](#2) )
   IMPSUMMARY[topic=+](#2).
  
if ( KEYWORD(#1) & ^IMPSENT[topic:~](#2) )
   IMPSENT[topic=+](#2).

if ( ^IMPSENT[topic:~](#1) )
   ~.
  
if ( ^IMPSUMMARY[topic:~](#1) )
   ~.
  
if ( ^SUMMARY[topic:~](#1) )
   ~.

///////////////////////////////////////////
\ 



////////////////////////
// counting the number of SENT with topicwords and the number of SUMMARY and IMPSUMMARY with topicwords


if ( ^SUMMARY(#1) & KEYWORD(#2) )
   SUMMARY[topic=+](#1).

if ( ^IMPSUMMARY(#1) & KEYWORD(#2) )
   IMPSUMMARY[topic=+](#1).

if ( ^IMPSENT(#1) & KEYWORD(#2) )
   IMPSENT[topic=+](#1).

if ( ^SENT(#1) & KEYWORD(#2) )
   SENT[topic=+](#1).

Script:

iftest ( KEYWORD(#2) ) { #2[topicword=+] ; }

//iftest ( SENT(#1) & KEYWORD(#2) ) { #2[topicword=~] ; }


/////////////////////////////////////
// Word frequency
////////////////////////////////////
/
Script:

iftest ( KEYWORD(#2) ) { #2[topicword=~] ; }
\
DependencyRules:

| #1[noun,pron:~] |
WORD(#1).

if ( ^WORD(#1) & WORD(#2) &#1::#2 )
   ~.

if ( ^WORD(#2) & HEAD(?,#2) )
   ~.

if ( ^WORD(#1) & KEYWORD(#1) )
   WORD[topic=+](#1).




if ( ^IMPSENT(#1) & WORD[abstractword](#2) )
   IMPSENT[abstractword=+](#1)


if ( ^IMPSUMMARY(#1) & WORD[abstractword](#2) )
   IMPSUMMARY[abstractword=+](#1)

if ( ^SUMMARY(#1) & WORD[abstractword](#2) )
   SUMMARY[abstractword=+](#1)

if ( ^SENT(#1) & WORD[abstractword](#2) )
   SENT[abstractword=+](#1)




| #2[topicword=+] |
if ( IMPSENT[topic](#1) & ( $2[ksw](#2,#3) | $2[ksw](#3,#2) ) )
   ~.

| #3[topicword=+] |
if ( IMPSENT[topic](#1) & ( $2[ksw](#2,#3) | $2[ksw](#3,#2) ) )
   ~.

| #1[md=+] |
if ( KSWDEP(#1,#2) & IMPSENT[vis](#3) )
   ~.
| #1[md=+] |
if ( KSWDEP(#1,#2) & IMPSENT[weakcontr](#3) )
   ~.

| #1[md=+] |
if ( KSWDEP(#1,#2) & IMPSENT[nostat](#3) )
   ~.

| #2[md=+] |
if ( KSWDEP(#1,#2) & IMPSENT[vis](#3) )
   ~.

| #2[md=+] |
if ( KSWDEP(#1,#2) & IMPSENT[weakcontr](#3) )
   ~.

| #2[md=+] |
if ( KSWDEP(#1,#2) & IMPSENT[nostat](#3) )
   ~.


| #2[md=+] |
if ( KSWDEP(#2) & IMPSENT[vis](#3) )
   ~.

| #1[md=+] |
if ( KSWDEP(#1,#2) & IMPSUMMARY[vis](#3) )
   ~.

| #1[md=+] |
if ( KSWDEP(#1,#2) & IMPSUMMARY[weakcontr](#3) )
   ~.

| #1[md=+] |
if ( KSWDEP(#1,#2) & IMPSUMMARY[nostat](#3) )
   ~.

| #2[md=+] |
if ( KSWDEP(#1,#2) & IMPSUMMARY[vis](#3) )
   ~.

| #2[md=+] |
if ( KSWDEP(#1,#2) & IMPSUMMARY[weakcontr](#3) )
   ~.

| #2[md=+] |
if ( KSWDEP(#1,#2) & IMPSUMMARY[nostat](#3) )
   ~.

| #2[md=+] |
if ( KSWDEP(#2) & IMPSUMMARY[vis](#3) )
   ~.

| #2[md=+] |
if ( KSWDEP(#2) & IMPSUMMARY[weakcontr](#3) )
   ~.

| #2[md=+] |
if ( KSWDEP(#2) & IMPSUMMARY[nostat](#3) )
   ~.


| #1[md=+] |
if ( $1[contribution](#1,#2) )
 ~.

| #2[md=+] |
if ( $1[contribution](#1,#2) )
 ~.

| #2[md=+] |
if ( $1[contribution](#2) )
 ~.

| #1[md=+] |
if ( KSWDEP(#1) & IMPSENT[vis](#2) )
   ~.

| #1[md=+] |
if ( KSWDEP(#1) & IMPSENT[weakcontr](#2) )
   ~.

| #1[md=+] |
if ( KSWDEP(#1) & IMPSENT[nostat](#2) )
   ~.

| TOP#1[md=~]{ ?+ } |
 ~.



| #4noun[md]{#1[md=+],#2[md=+],#3[md=+],#4[md=+]} |
  ~.

| #4noun[md]{#1[md=+],#2[md=+],#3[md=+]} |
  ~.

| #4noun[md]{#1[md=+],#2[md=+]} |
  ~.

//////////////////////////////////////////////

