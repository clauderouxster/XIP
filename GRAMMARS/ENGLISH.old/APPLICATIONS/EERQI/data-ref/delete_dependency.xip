DependencyRules:

// Some dependencies tend to be erroneous and at the same time to useful for concept matching: EMBED(#1,#2) and $1(#2,#1)

if ( EMBED(#1,#2) & ^$1(#2,#1) )
	~.

if ( ^EMBED(#1,#2) )
	~.

