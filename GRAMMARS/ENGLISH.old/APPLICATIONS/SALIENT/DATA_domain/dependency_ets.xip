DependencyRules:

| ?[md,mental] |
if (^PROBLEM(#1) )
   PROBLEM[mental=+](#1).
   
| ?[md,scope] |
if (^PROBLEM(#1) )
   PROBLEM[scope=+](#1).
   
| ?[md,timefact] |
if (^PROBLEM(#1) )
   PROBLEM[timefact=+](#1).

| ?[md,attitude] |
if (^PROBLEM(#1) )
   PROBLEM[attitude=+](#1).

| ?[md,importance] |
if (^PROBLEM(#1) )
   PROBLEM[importance=+](#1).

| ?[md,contr] |
if (^PROBLEM(#1) )
   PROBLEM[contr=+](#1).
   
| ?[md,change] |
if (^PROBLEM(#1) )
   PROBLEM[change=+](#1).
   
| ?[md,publ] |
if (^PROBLEM(#1) )
   PROBLEM[publ=+](#1).
   
////////////

| ?[md,mental] |
if (^PROBLEM-SUMMARY(#1) )
   PROBLEM-SUMMARY[mental=+](#1).
   
| ?[md,scope] |
if (^PROBLEM-SUMMARY(#1) )
   PROBLEM-SUMMARY[scope=+](#1).
   
| ?[md,timefact] |
if (^PROBLEM-SUMMARY(#1) )
   PROBLEM-SUMMARY[timefact=+](#1).

| ?[md,attitude] |
if (^PROBLEM-SUMMARY(#1) )
   PROBLEM-SUMMARY[attitude=+](#1).

| ?[md,importance] |
if (^PROBLEM-SUMMARY(#1) )
   PROBLEM-SUMMARY[importance=+](#1).

| ?[md,contr] |
if (^PROBLEM-SUMMARY(#1) )
   PROBLEM-SUMMARY[contr=+](#1).
   
| ?[md,change] |
if (^PROBLEM-SUMMARY(#1) )
   PROBLEM-SUMMARY[change=+](#1).
   
| ?[md,publ] |
if (^PROBLEM-SUMMARY(#1) )
   PROBLEM-SUMMARY[publ=+](#1).
   
