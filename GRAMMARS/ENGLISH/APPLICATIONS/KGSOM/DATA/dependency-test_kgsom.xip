DependencyRules:

//against firstturn

| #1[tb] |
if ( ^ANOMALY(#2) )
   ANOMALY[tb=+](#2).

| #1[tb] |
if ( ^SENT(#2) )
   SENT[tb=+](#2).

| #1[tb] |
if ( ^QUESTION(#2) )
   QUESTION[tb=+](#2).

| #1[sp] |
if ( ^ANOMALY(#2) )
   ANOMALY[sp=+](#2).

| #1[sp] |
if ( ^SENT(#2) )
   SENT[sp=+](#2).

| #1[sp] |
if ( ^QUESTION(#2) )
   QUESTION[sp=+](#2).

| #1[oth] |
if ( ^ANOMALY(#2) )
   ANOMALY[oth=+](#2).

| #1[oth] |
if ( ^SENT(#2) )
   SENT[oth=+](#2).

| #1[oth] |
if ( ^QUESTION(#2) )
   QUESTION[oth=+](#2).


| #1[na] |
if ( ^ANOMALY(#2) )
   ANOMALY[na=+](#2).

| #1[na] |
if ( ^SENT(#2) )
   SENT[na=+](#2).

| #1[na] |
if ( ^QUESTION(#2) )
   QUESTION[na=+](#2).

| #1[ht] |
if ( ^ANOMALY(#2) )
   ANOMALY[ht=+](#2).

| #1[ht] |
if ( ^SENT(#2) )
   SENT[ht=+](#2).

| #1[ht] |
if ( ^QUESTION(#2) )
   QUESTION[ht=+](#2).