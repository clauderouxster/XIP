// XML Guide for WSJ XML documents


OpenMarkup:
	// nettoyage des variables a chaque nouvelle balise DOC
//	if (@_current(DSDSD)->test()){print("nettoyage variables \n");}


//1> if(@_current(DOC)->test()){cleanalllemmas;print("nettoyage variables \n");}




XMLGuide:
#default->Analyze(dependency).
IN->Skip(all).
HL->Skip(all).
DATELINE->Skip(all).
DD->Skip(all).
DOCID->Skip(all).
DATE->Skip(all).
CO->Skip(all).
RE->Skip(all).
IN->Skip(all).
GV->Skip(all).


