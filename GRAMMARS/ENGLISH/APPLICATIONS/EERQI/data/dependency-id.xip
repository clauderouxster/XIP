DependencyRules:


| #1[adj] |
if ( ~P(#1) & ~HEAD(#2,#1) )
   P(#1).

| #1[conj] |
if ( ~P(#1) )
   P(#1).

| #1[verb] |
if ( ~P(#1)  )
   P(#1).
   

| PREP#1[prep] |
if ( ~P(#1)  )
   P(#1).

| #1[adv,lemme:~not] |
if ( ~P(#1)  )
   P(#1).

| #1[punct:~] |
if ( ~P(#1) & ~HEAD(#2,#1) )
   W(#1).

| noun#4{#1,#2,#3} |
if ( ^W(#1) )
   ~.

| noun#4{#1,#2,#3} |
if ( ^P(#1) )
   ~.

| noun#4{#1,#2,#3} |
if ( ^W(#2) )
   ~.

| noun#4{#1,#2,#3} |
if ( ^P(#2) )
   ~.

| noun#4{#1,#2,#3} |
if ( ^W(#3) )
   ~.

| noun#4{#1,#2,#3} |
if ( ^P(#3) )
   ~.

| TOP#1 |
if ( ^W(#1) )
   ~.

| SC#1 |
if ( ^P(#1) )
   ~.

| GV#1 |
if ( ^P(#1) )
   ~.

| FV#1 |
if ( ^P(#1) )
   ~.

| NFV#1 |
if ( ^P(#1) )
   ~.

| IV#1 |
if ( ^P(#1) )
   ~.

if ( ^P(#1) & $1[vlink](#1,#2) )
   ~.

   
| #1{#2, #3} |
if ( ^P(#1) & P(#3) )
   ~.  

| #1{#2, #3} |
if ( ^P(#1) & W(#3) )
   ~.  

| #1{#2, #3} |
if ( ^W(#1) & W(#3) )
   ~.  

| #1{#2, #3} |
if ( ^W(#1) & P(#3) )
   ~.  
