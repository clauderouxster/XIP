DependencyRules:

| TOP#1{ ?+ } |
  SENT(#1).

| #1[sufix_company] |
if (^SENT(#2) )
  ORGANIZACION(#2).


| #1[potzipcode], #2[prop] |
if (^SENT(#3) )
  ADDRESS(#3).

| #1[address] |
if (^SENT(#3) )
  ADDRESS(#3).


