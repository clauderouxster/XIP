DependencyRules:
/
//begin visual_saliency

| #1[lemme:framework ] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


| #1[lemme:visual ] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


| #1[lemme:saliency ] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


| #1[lemme:detection ] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


| #1[lemme:application] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


| #1[lemme:image ] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


| #1[lemme:thumbnail] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


| #1[lemme:thumbnailing] |
if ( ~KEYWORD(#1) )
   KEYWORD(#1)


// end visual_saliency

\