Features:
[mestraits: [leterme:+,lexpression:+]]


Functions:
monterme,
monexpression.


DependencyRules:

|#1[leterme]|
   monterme(#1).

|#1[lexpression]|
  monexpression(#1).

  
XMLGuide:

#default->skip(text).
p->merge(dependency).
terme->feature(leterme,leterme).
expression->feature(lexpression,lexpression).
