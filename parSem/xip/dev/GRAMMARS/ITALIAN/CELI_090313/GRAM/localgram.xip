// localgram.xip
// local grammar for italian
// created: 25.8.2008 (Sigrid Maurel, CELI France)
// modified 27.8.2008 (Sigrid Maurel, CELI France)


Sequence:

// 25.8.2008 (SM):
// bell'
1> adj[adj=+, sg=+, masc=+, surface=bello] = ?[surface:bell], symbol[lemma:"'"].

// 25.8.2008 (SM):
// sessant'otto
1> num = num[elided:+], num.

// 27.8.2008 (SM):
// un po'
1> quant = num[apoc:+, lemma:uno], quant[apoc:+, lemma:poco].

// 13.11.2008 (SM):
// se non altro
1> adv = ?[lemma:se], neg[lemma:non], adj[lemma:altro].

// 9.12.2008 (SM):
// in mezzo a
//1> prep = prep[lemma:in], noun[lemma:mezzo], prep[lemma:a].

