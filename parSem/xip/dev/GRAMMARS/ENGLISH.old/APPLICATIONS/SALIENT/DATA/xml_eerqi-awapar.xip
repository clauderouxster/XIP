XMLGuide:

#default->skip(text).

article->analyze(dependency).
title->analyze(dependency).
abstract->analyze(dependency).
body->analyze(dependency).
region->analyze(dependency).
awapara->feature(begpar,par).

tcontext->feature(begtcontext,tcontext).
tchallenge->feature(begtchallenge,tchallenge).
tchange->feature(begtchange,tchange).
tsalient->feature(begtsalient,tsalient).
xcontext->feature(begxcontext,xcontext).
xchallenge->feature(begxchallenge,xchallenge).
xchange->feature(begxchange,xchange).
xsalient->feature(begxsalient,xsalient).







