// XML Guide for Reuters XML documents





XMLGuide:
#default->Analyze(dependency).
DATE->Skip(all).
TOPICS->Skip(all).
PLACES->Skip(all).
PEOPLE->Skip(all).
ORGS->Skip(all).
EXCHANGE->Skip(all).
COMPANIES->Skip(all).
UNKNOWN->Skip(all).
TITLE->Skip(all).
DATELINE->Skip(all).


