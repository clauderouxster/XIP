DependencyRules:

if ( ^GEOGRAFICO(#1) )
   LOCATION(#1).

if ( ^ORGANIZACION(#1) )
   ORGANISATION(#1).

if ( ^FECHA(#1) )
   DATE(#1).

if ( ^EVENTO(#1) )
   EVENT(#1).

if (^PERSONA(#1) )
   PERSON(#1).

