
<project>

<!--     common properties -->
    <tstamp prefix="curDate">
        <format property="now" pattern="dd-MMMM-yyyy" locale="en"/>
        <format property="nowFull" pattern="dd-MMMM-yyyy  hh:mm" locale="en"/>
    </tstamp>
    <property name="fstOrTrans.dir"         value="xipFst" />
    <property name="lib.dirs"               location="${basedir}/lib" />
    <property name="obj.dirs"               location="${basedir}/obj" />
    <property name="bin.dirs"               location="${basedir}/bin" />
    <property name="log.dirs"               location="${basedir}/log" />
    <property name="include.dir"            location="${basedir}/include" />
    <property name="source.dir"             location="${basedir}/src" />
    <property name="sample.dir"             location="${basedir}/sample" />
    <property name="doc.dir"                location="${basedir}/doc" />
    <property name="test.dir"               location="${basedir}/test" />
    <property name="delivery.root"          location="${basedir}/../delivery" />
    <property name="archive.root"           location="${basedir}/../archive" />
    <property name="user.name"              value="env.USER" />
</project>
