<project>
    <target name="initLock">
    	<property name="lockJavaTaskSync.fileName" value="${common.dir}/../logs/xipjar.lock" />
    	<property name="lockJavaTaskSync.name" value="${lockJavaTaskSync.fileName}" />
    </target>

    <target name="cleanLock" depends="initLock">
	<delete file="${lockJavaTaskSync.fileName}" />
    </target>
</project>

