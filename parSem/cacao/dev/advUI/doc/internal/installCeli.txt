
1) ABSOLUMENT NECESSAIRE: changer log4j.properties    supprimer log

2) ABSOLUMENT NECESSAIRE: enlever le mode debug wicket: modifier advUI/web/WEB-INF/web.xml   
		set configuraton = deployment 
		sinon on utilise pas la bonne URL pour acceder au WS: Application.init() test si wicket est 
			en mode dev et il change l'url des WS

3) verifier DB embedded
