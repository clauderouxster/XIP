CREATE TABLE ITEM (ID BIGINT NOT NULL, LABEL VARCHAR(300) NOT NULL, ddepth VARCHAR(30) NOT NULL, ITEMID VARCHAR(30) NOT NULL, PARENTID VARCHAR(30) NOT NULL, classification_id BIGINT NOT NULL, PRIMARY KEY (ID))
CREATE TABLE CLASSIFICATION (ID BIGINT NOT NULL, NAME VARCHAR(30) UNIQUE NOT NULL, PRIMARY KEY (ID))
ALTER TABLE ITEM ADD CONSTRAINT TMclassificationid FOREIGN KEY (classification_id) REFERENCES CLASSIFICATION (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL, PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 1)
